NODE_ENV=development

# Server
TZ=UTC
HTTP_PORT=8080

# Logging
LOG_LEVEL=debug
LOG_PRINT_PRETTY=true

# Postgres Database
POSTGRES_HOST=localhost
POSTGRES_NAME=shoplog
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_NAME}?schema=public

# Sql Server Database
SQL_SERVER_HOST=localhost
SQL_SERVER_DATABASE=vPICList_Lite1
SQL_SERVER_PORT=1433
SQL_SERVER_USERNAME=sa
SQL_SERVER_PASSWORD=yourStrong(!)Password

# Redis

# Auth0
AUTH0_ISSUER=https://shoplog-dev.us.auth0.com/
AUTH0_AUDIENCE=shoplog
